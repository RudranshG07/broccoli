# Deploy to Vercel

1. Push to GitHub
2. Import repo on vercel.com
3. Set root directory: `frontend`
4. Add env var: `VITE_PINATA_JWT` (get from pinata.cloud)
5. Deploy

Done. GPU worker only works locally.
